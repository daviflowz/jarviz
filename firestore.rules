rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Regras completamente permissivas para resolver erro 400
    match /{document=**} {
      allow read, write: if true;
    }
  }
} 